/* eslint-disable @typescript-eslint/no-explicit-any */
"use client";
import ProfilClient from "../app/profile/[id]/page";
export default function ProfilClientModal({}: { id: any }) {
  return <ProfilClient />;
}
